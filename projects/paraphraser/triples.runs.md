# Stage B triple-generation runs

Every output row carries a `run_id`. This log records the parameters behind each id, so the
per-row provenance tags are decodable later. Generated by `generate_triples_workflow.js` /
`generate_triples_batched_workflow.js` (Claude Code Workflow) unless noted otherwise.

Provenance tags on each row: `tier`, `gen_model`, `gen_backend`, `prompt_version`, `run_id`, `gen_timestamp`.

## Current state (reconciled 2026-06-19)

Authoritative numbers, computed from `data/triples.claude-code.jsonl` against `data/run_manifest.jsonl`.
**All coverage is normalized** (collapse quotes/whitespace + insert missing space after sentence
punctuation): the stored `source` is the model's *echo*, so raw-text matching against the manifest
undercounts. An earlier 2026-06-18 pass that skipped normalization reported 39.8%/"heavy duplication" —
**both were artifacts of comparing echoed text to canonical text; corrected below.**

- **Output rows: 20,492** across 18 run_ids.
- **Distinct `(source, instruction)` pairs: 20,121 — only 371 true duplicates (1.8%).** The `--done-file`
  dedup in the re-emits worked well; there is *no* heavy duplication. `filter_triples.py --dedupe` removes the 371.
- **Manifest jobs covered: 4,813 / 10,000 = 48.1%** (≈5,043 distinct jobs generated). Remaining: ~5,187.
  - **easy: 3,464 / 5,000 (69.3%) done.**
  - **hard: 1,349 / 5,000 (27.0%) done.**
- **Tier skew is real:** earlier emits leaned *easy*, so `hard` trails. The `…013508Z` queue concentrates the
  hard backlog in chunks 004–009 — **run those next.**
- **Filtered/deduped corpus (as of chunk_003):** `filter_triples.py --dedupe --bertscore` → **19,891 kept**
  (99% of 20,121 distinct), 230 hard-dropped, 5,418 in the flagged file for review (incl. 3,606 BERTScore
  soft-flags + 3,564 number-drop soft-flags). See "Filter runs" below.

## How to run the chunks (READ FIRST)

**Do NOT launch all chunks in parallel.** Each chunk is ~80–100 subagents × ~430 tok/triple × ~2,900 triples ≈ **1.3M+ subagent tokens per chunk**. Firing the whole `data/runs/batched/` set at once would burn **>10M tokens in minutes**, blow through rate limits, and give no chance to inspect output between runs.

Run procedure:

1. **One chunk at a time, sequentially.** Invoke a single chunk, wait for it to finish, then persist and inspect before starting the next:
   ```
   Workflow({ scriptPath: "<abs>/projects/paraphraser/data/runs/batched/chunk_NNN.workflow.js" })
   ```
2. **At most 1–2 chunks in flight**, never the full set. If you parallelize at all, cap it at 2 and watch the token meter.
3. **Persist after each chunk** — append the returned `triples` to `data/triples.claude-code.jsonl` (snippet in the workflow header). The result is in-memory only until you write it.
4. **Check `failed` / `mismapped`** in the return value before moving on. Re-run a chunk only if `failed` is non-trivial.
5. **Append a row to this log** for the run_id you just completed.
6. **Re-emit, don't hand-edit**, when remaining work changes: `python3 emit_chunks_batched.py` with the live `--done-file` repacks only outstanding jobs (and would fix the current overlap + tier skew).

The subagent-concurrency cap inside a single Workflow already limits fan-out *within* one chunk; the rule above is about not stacking *chunks* on top of each other.

## Run log

Rows grouped by emit generation (the timestamp suffix shared by a generation's `run_id`s).

| run_id(s) | date (UTC) | backend | teacher model(s) | rows | distinct jobs | output file | notes |
|---|---|---|---|---|---|---|---|
| `claude-pilot-20260617T112732Z` | 2026-06-17 | claude-code-workflow | haiku-4.5 (easy) / sonnet-4.6 (hard) | 60 | 12 | `data/triples.claude-code.jsonl` | First pilot; prompt_version v1. Filtered with `filter_triples.py` v1 → **59/60 kept** (1 dropped: 11-word miss; 3 number-retention soft-flags, all the quantum "two particles" → "paired"/"partner"). BERTScore layer not run. Chiasmus quality varies even on Sonnet. |
| `claude-bulk-001-20260618T011722Z` | 2026-06-18 | claude-code-workflow | haiku-4.5 / sonnet-4.6 | 2560 | 641 | `data/triples.claude-code.jsonl` | First bulk chunk (non-batched, chunk_001 of 16). 640 subagents, **0 failed**, all 4/4. **7,169,303 tokens** ≈ 2,800 tok/triple. Wall clock ~20 min. prompt_version v1. |
| `claude-batch8-001-20260618T015652Z` | 2026-06-18 | claude-code-workflow (BATCHED) | haiku-4.5 / sonnet-4.6 | 3072 | 769 | `data/triples.claude-code.jsonl` | First **batched** chunk (batch-8 verification gate) via `generate_triples_batched_workflow.js`. 96 subagents, **0 failed**, all 32/32. **1,327,155 tokens = ~432 tok/triple** (85% cut vs single-source). `mismapped`=32 all **false positives** (single→double quote swap at char ~39; provenance verified correct). Detector since hardened (normalize quotes/whitespace, 60-char prefix). prompt_version v1-batch. |
| `claude-batch8-001..012-20260618T020948Z` (12 run_ids) | 2026-06-18 | claude-code-workflow (BATCHED) | haiku-4.5 / sonnet-4.6 | 5900 | 1479 | `data/triples.claude-code.jsonl` | **Reconciliation correction:** the prior log entry called these "re-emitted as 12 batched chunks — paused, await go-ahead." They were in fact **run** — all 12 chunks executed and landed 5,900 rows. Per-chunk rows: 672/608/736/800/960/396/288/224/192/320/320/384. Mixed tiers but mostly easy. prompt_version v1-batch. |
| `claude-batch8-001..002-20260618T181317Z` (2 run_ids) | 2026-06-18 | claude-code-workflow (BATCHED) | haiku-4.5 (all easy this gen) | 5920 | 1486 | `data/triples.claude-code.jsonl` | Superseded emit; chunks deleted. **Only chunks 001 (2,944 rows) & 002 (2,976 rows) ran** — both all-easy, 0 failed. This generation re-ran easy work already covered earlier (dedup miss) — main source of the duplication + tier skew above. prompt_version v1-batch. |
| `claude-batch8-003-20260619T013508Z` | 2026-06-19 | claude-code-workflow (BATCHED) | haiku-4.5 (easy) / sonnet-4.6 (hard) | 2980 | — | `data/triples.claude-code.jsonl` | First chunk of the clean `…013508Z` re-emit (568 hard + 177 easy sources). 94 subagents, **0 failed**, all 8/8. **1,342,733 tokens = ~451 tok/triple.** Wall clock ~16 min. `mismapped`=138 (all easy) **all false positives**: seed text has a detokenizer artifact (missing space after sentence period, e.g. `"staring.he"`) which Haiku silently repaired when echoing (`"staring. he"`); the detector normalizes whitespace but doesn't *insert* a missing space, so the 60-char prefix diverges. **0 real cross-contamination** (verified against manifest). prompt_version v1-batch. Persisted; live file now 20,492 rows. |
| `claude-batch16-003-20260621T011634Z` | 2026-06-21 | claude-code-workflow (BATCHED) | haiku-4.5 (easy) / sonnet-4.6 (hard) | 2792 | 698 | `data/triples.claude-code.jsonl` | First **batch-16** chunk (sources/subagent doubled from 8; from a fresh `…011634Z` re-emit against the live done-file). 44 subagents, **0 failed**. **642,011 tokens = ~230 tok/triple — ≈half of batch-8's ~451**, confirming the amortization holds at 16. Wall clock ~20 min. `mismapped`=196 (60 easy/dialogue + 136 hard/news) **all false positives** — spot-checked targets each match their own source; the mapping check over-flags on echo divergence (these short lines have no missing-space artifact, a *different* benign cause than chunk_003 above). prompt_version v1-batch. Persisted; live file now 23,284 rows. |
| `claude-batch16-005-20260621T122211Z` | 2026-06-21 | claude-code-workflow (BATCHED) | haiku-4.5 (easy) / sonnet-4.6 (hard) | 1360 | 340 | `data/triples.claude-code.jsonl` | First chunk of the **smaller `…122211Z` re-emit** (`--max-bytes 250000` → ~1,400 triples/chunk; head of the hard backlog, 272 hard + 68 easy sources). 22 subagents, **0 failed**. **327,220 tokens = ~240 tok/triple.** Wall clock ~17 min. `mismapped`=268 (164 easy/dialogue + 104 hard/news; ~20%, up from prior chunks) **all false positives** — spot-checked targets each match their own source; the mapping check is just noisy on short dialogue/news echoes. prompt_version v1-batch. Persisted; live file now 24,644 rows. |
| `claude-batch16-006-20260621T122211Z` | 2026-06-21 | claude-code-workflow (BATCHED) | sonnet-4.6 (all hard) | 1344 | 336 | `data/triples.claude-code.jsonl` | Second `…122211Z` chunk; first **pure-hard** one (336 hard sources → all Sonnet). 21 subagents, **0 failed**. **306,604 tokens = ~228 tok/triple.** Wall clock ~11 min. `mismapped`=16 (all hard/news) **all false positives** — far fewer than chunk_005 because long news sources rarely trip the echo detector. prompt_version v1-batch. Persisted; live file now 25,988 rows. |
| `claude-batch16-007-20260621T122211Z` | 2026-06-21 | claude-code-workflow (BATCHED) | sonnet-4.6 (all hard) | 1344 | 336 | `data/triples.claude-code.jsonl` | Third `…122211Z` chunk; pure-hard (336 hard sources, mostly arxiv/news → Sonnet). 21 subagents, **0 failed**. **306,956 tokens = ~228 tok/triple.** Wall clock ~10 min. `mismapped`=4 (all arxiv) **all false positives** — long technical sources almost never trip the echo detector. prompt_version v1-batch. Persisted; live file now 27,332 rows. |

## Filter runs

| date | command | input | kept | dropped | flagged | notes |
|---|---|---|---|---|---|---|
| 2026-06-19 | `filter_triples.py --dedupe --bertscore` | 20,492 → 20,121 distinct | 19,891 (99%) | 230 hard | 5,418 | First run with `bert_score` (roberta-large, MPS, rescaled). Hard drops: exact-word-count misses (208) + one-sentence (20) + max-words (4) + declarative (2). Soft flags: 3,564 number-drop, 3,606 BERTScore. |
| 2026-06-21 | `filter_triples.py --dedupe --bertscore` | 24,644 → 23,742 distinct (902 dupes) | 23,480 (99%) | 262 hard | 6,829 | After persisting chunk_003 (`…011634Z`) + chunk_005 (`…122211Z`). BERTScore measured **~13 pairs/sec on MPS → ~32 min** for the full pass. Hard drops: exact-word-count misses (234) + one-sentence (22) + max-12 (4) + declarative (2). Soft flags: 4,280 number-drop, 4,687 BERTScore. **F1 distribution stable vs 2026-06-19** (strict median 0.47→0.45, lenient 0.36→0.35) — new chunks as faithful as the earlier corpus. **Strict threshold 0.45 still sits at the strict-group median (n=4,173), flagging 50%** — over-flags legit compression rewrites; lower to ~0.30 before relying on `--strict`. Lenient 0.15 flags bottom 13% (n=19,307), healthy. |

**BERTScore threshold tuning needed.** Distribution of kept rows' F1 by axis group:
- **strict** (length/structural, threshold 0.45): n=3,129, median **0.47**, p10 0.22 — the threshold sits *at* the median, so it soft-flags ~half of these. Compression instructions ("in exactly five words") legitimately lose surface overlap; 0.45 is likely too high. Consider lowering strict to ~0.30, or exempting pure length-compression axes.
- **lenient** (everything else, threshold 0.15): n=16,762, median 0.36, p10 0.12 — flags the bottom ~12%, which looks reasonable.

Flags are **soft** (kept, not dropped) unless `--strict` is passed, so these don't affect the trainable `filtered.jsonl` yet — but the thresholds should be tuned before anyone relies on `--strict`.

### Pending work — fresh queue emitted `20260621T122211Z` (batch-16, smaller chunks)

Re-emitted at **`--batch-sources 16 --max-bytes 250000`** against the live done-file (now 5,713 (source,tier) done = 57%): `emit_chunks_batched.py --batch-sources 16 --label batch16 --max-bytes 250000` repacked the **4,793 remaining jobs** into **14 chunks** under run_id stem `claude-batch16-NNN-20260621T122211Z`. Chunks were halved from the prior ~493 KB / 2,792-triple size to **~1,400 triples / ~10 min / ~320 K tokens each**, so a full run→persist→verify→log cycle fits in one session. The stale `…011634Z` chunks were deleted; `data/runs/batched/` holds only this queue.

Chunks are tier-grouped, so the **hard backlog (3,141 sources) is concentrated in chunks 005–014** — run those first to keep closing the tier gap (hard still trails easy):

| chunk | subagents | sources | easy | hard | tier |
|---|---|---|---|---|---|
| chunk_001 | 23 | 368 | 368 | 0 | easy |
| chunk_002 | 25 | 400 | 400 | 0 | easy |
| chunk_003 | 25 | 400 | 400 | 0 | easy |
| chunk_004 | 26 | 416 | 416 | 0 | easy |
| chunk_005 | 22 | 340 | 68 | 272 | MIXED — head of hard |
| chunk_006 | 21 | 336 | 0 | 336 | hard |
| chunk_007 | 21 | 336 | 0 | 336 | hard |
| chunk_008 | 21 | 336 | 0 | 336 | hard |
| chunk_009 | 22 | 352 | 0 | 352 | hard |
| chunk_010 | 23 | 368 | 0 | 368 | hard |
| chunk_011 | 23 | 368 | 0 | 368 | hard |
| chunk_012 | 24 | 384 | 0 | 384 | hard |
| chunk_013 | 24 | 384 | 0 | 384 | hard |
| chunk_014 | 1 | 5 | 0 | 5 | hard |
| **total** | **301** | **4793** | **1652** | **3141** | |

Run **one chunk at a time** per the procedure above; persist after each, then append its run_id row to this log. Start at **chunk_005** (first chunk with hard work).

<!-- (legacy) Qwen rows in data/triples.jsonl (18) + review/triples_review_sample.jsonl predate the
     provenance schema and carry no run_id. Backfill pending if uniform traceability is wanted. -->
